Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On

    # JIKA FILE ASLI ADA (global.php, css, js), JANGAN REWRITE
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]
</IfModule>
